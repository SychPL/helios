# Helios 0.8.7 + ha-helios 0.7.5 - tła i wygląd per urządzenie (SPEC 0.8b; plan etap B) oraz poprawki z odbioru

- Baza: 0.8.6, ha-helios 0.7.4. Plan: docs/PLAN-0.8-implementation.md (B0-B3).
- **B0 (bramka) NIE zaliczona** - wymaga próby options flow na HA 2026.8.3 użytkownika (FileSelector z aplikacji Companion i z przeglądarki). Kod jest gotowy i wypchnięty na `main` SychPL/ha-helios bez tagu wydania; HACS nie zaktualizuje instalacji, dopóki nie powstanie tag `v0.7.5` - dopiero po bramce.
- B1 ha-helios: `appearance.py` (walidacja, `prepare_image` Pillow: EXIF transpose, nowy obraz z pikseli bez metadanych, spłaszczenie alfa na #181C24, 1600×960, JPEG ≤ 2 MiB, odrzucenie GIF/animacji/>10 MiB/>24 Mpx/>8192/uszkodzonych; `repair_storage` samonaprawa pary plik-opcje), `HeliosOptionsFlow` (kroki `init` i `upload`, `float→int`, lock per wpis, plik `<image_id>.jpg` zapisany przed opcjami, poprzedni plik zostaje do następnego uploadu), `HeliosAppearanceView` GET `/api/helios/appearance/{entry_id}/{image_id}` (auth, właściciel lub admin, tylko bieżący id, 404 dla obcych), snapshot `appearance` po `connected` i z update listenera (bez reload), `async_remove_entry` usuwa katalog. Testy: 7 pytest (walidacja, obraz, naprawa) - CI hassfest/hacs/pytest zielone dla 0.7.5.
- B2 zegar: `Appearance.parse` (ścisła walidacja wersji, motywu, id, ścieżki, zakresów), `CropMath` (kadr z punktem bez pasów), `HeliosDeviceClient` zdarzenie `appearance`, serwis: motyw natychmiast, obraz po pobraniu (origin z parowania + ścieżka ze snapshotu, Bearer w nagłówku, bez przekierowań, 2 MiB, 10 s, cache `files/background.jpg` per `image_id`, ten sam obraz z nowym kadrem tylko dekodowany ponownie, stara bitmapa zostaje do czasu nowej), prefs `appearance`, unieważnienie przy innym `device_id` w `connected` i przy zmianie HA w `reconfigure`. `DashboardView`: warstwa zdjęcia + osłona dim, pasek 100 %, kafle 92 % powierzchni, zegar 70 % czerni, bez przebudowy siatki; muzyka i połączenia nietknięte.
- Poprawki z odbioru użytkownika (poza planem, 2026-09-15): (1) Assist „wyłącz muzykę”: `media_player` Heliosa ma PLAY/PAUSE/STOP/TURN_OFF → komendy `music.play|pause|stop` w allowliście (HA i zegar), wykonywane jak przyciski panelu; `music_state` w telemetrii; (2) mignięcie lampki docka przy rozpoczęciu słuchania komendy (`setVoiceState("listening")`, 180 ms, lampka włączona mruga wyłączeniem); (3) dotknięcie ekranu podnosi jasność wyświetlacza o 20 pkt na 15 s (`screenBrightness` okna, bazowo z ustawień systemu).
- Build, lint (0 błędów), 62 testy JVM: PASS (nowe: `AppearanceTest` 3, `ArtworkLoaderTest` +1, `HeliosDeviceClientTest` +2).
- Emulator (fixture HA `--v3 --hitbox --photo`: snapshot `appearance` night_blue + zdjęcie pasmowe z endpointu wymagającego Bearer): motyw Nocny błękit zastosowany od razu, zdjęcie pod siatką z przyciemnieniem 50 %, kafle 92 %, zegar z osłoną, pasek nieprzezroczysty; uchwyt i panel muzyki nad zdjęciem (panel 100 % powierzchni, okładka i akcent), telemetria `music_state: playing`. Zrzuty: `.local/shots-087/*.png`. Fixture: 401 bez tokenu (kod) - sprawdzone tylko ścieżką pozytywną z tokenem.
- Nie sprawdzono: prawdziwe HA (options flow, FileSelector z telefonu, endpoint, restart HA po uploadzie), dwa zegary z różnymi tłami, 20 podmian obrazu i RAM, zapis wyglądu podczas grania na zegarze, mignięcie lampki i jasność na fizycznym Lenovo, „wyłącz muzykę” przez Nabu z prawdziwym MA.
- Diagnostyka emulatora: pobrania HTTP z fixture w Pythonie przez NAT emulatora gubiły ogon odpowiedzi (server zamykał socket zaraz po odpowiedzi) - obejście w fixture (linger, keep-alive); w aplikacji `http.keepAlive=false` (jednorazowe GET-y, pula OkHttp oddawała zamknięte gniazdo bez ponowienia).

APK serwowany przez most (port 8757, `/helios.apk`) z `app/build/outputs/apk/debug/app-debug.apk`.
SHA256: 4b5732ec223c628a1ee946c8b52ba2012c2c2723f0847844a9052cdbce4cd95b
