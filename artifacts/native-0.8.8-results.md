# Helios 0.8.8 - rolety sypialni, światło kontekstowe, pogoda na jutro (SPEC 0.9; plan etapy A-C)

- Baza: 0.8.7. Plan: docs/PLAN-0.9-implementation.md, zatwierdzony przez Codex po 6 rundach (rebutali brak).
- A1 schemat 4: `cover_group` (dokładnie dwie rolety `{entity,title}`), `weather.forecast_entity`+`forecast_when` (tylko razem, tylko v4), wspólna walidacja `{entity,state}`, `COVER_ATTRIBUTES` = `current_position`+`supported_features` dla całej domeny cover, siedem atrybutów prognozy; `EntityStates` usuwa atrybut przy JSON `null` (`templow` → brak minimum). Schematy 2/3 bez zmian; nowe pola w 2/3 odrzucane.
- B1 kafel Rolety: „A: zamknięta” / „B: 40%” (`CoverText`), pełne tytuły z YAML w opisie dostępności i panelu. B2 `Forecast`: rekord walidowany w całości (`ready`, kod warunków, liczby, `°C`/`°F`, data, ISO, `valid_until > teraz`), tryb z pomocnika (`unknown`/`unavailable` = „brak danych o trybie”), „Jutro” + „maks. 12°C” + „Deszcz · min. 4°C”, wygaśnięcie bez delty przez istniejący tick zegara. B3 panel „Rolety sypialni” (`CoverPanelGeometry` 720×368, dwa wiersze 96, przyciski 72×72, Wróć 72): klucz oczekiwania `id:encja:usługa`, `stop_cover` nigdy nie blokowany ani nie śledzony, przyciski z `supported_features`, niedostępna roleta wyłącza tylko swój wiersz, kafle `cover`/`cover_group` nie są wyłączane przez oczekiwanie (tylko spinner), niedostępne `light`/`garage`/`cover` są widoczne, ale nieaktywne. B4 kafle powiadomień (`entity` + `visible_when`) świecą na stałe pomarańczowo `#EDBE83` (obrys 2, ciepły ton 10 %, tekst i ikona) w obu motywach; offline bez obrysu.
- C1 `ha/packages/helios_bedroom.yaml`: `binary_sensor.helios_sypialnia_swiatlo_pokaz` (OR trzech członów, `availability`: prawdziwy człon albo komplet znanych), `binary_sensor.helios_pogoda_jutro_tryb` (`now().hour >= 18`), trigger-based `sensor.helios_pogoda_jutro` (`weather.get_forecasts daily` z `continue_on_error`, krok `variables.record` wybiera rekord po dacie lokalnej „jutro”, dwa rekordy = brak, poprzedni rekord z `this.attributes` tylko gdy data i `valid_until` aktualne, `valid_until` = min(północ lokalna, +6 h)). 16 testów pytest (`ha/tests/test_bedroom_package.py`, jinja2 z atrapami HA, strefa Europe/Warsaw, zmiana czasu 2026-10-25, koniec roku).
- C2 `tools/deploy_bedroom_dashboard.py` + manifest `ha/helios-bedroom.yaml`: preflight (pomocniki live, źródła istnieją i nie `unavailable`, rolety z open/close/stop, dokładny skład grupy światła i `all: false` z `suggested_value` options flow (flow usuwany bez zapisu), pomocniki tylko platformy `template` z `unique_id` `helios_*`, wersja APK z rejestru encji `platform: helios` / `unique_id *_app_version` ≥ 0.8.8, prognoza dzienna wspierana), transformacja (v4, kafle manifestu zastępują komórki (3,1-2),(4,1-2), reszta nietknięta), kopia, odczyt przed zapisem, readback, `--rollback` tylko sekcji `helios` po porównaniu z opublikowaną. Nie wysyła usług. `deploy_attention_dashboard.py` już przerywa przy schemacie innym niż 2.
- Build, lint (0 błędów), 76 testów JVM: PASS (nowe: `DashboardSpecTest` +2, `CoverTextTest` 2, `ForecastTest` 4, `CoverPanelGeometryTest` 2, `HaDashboardClientTest` +1, `ThemeTest` rozszerzony).
- Emulator 800×480 (fixture `--v4` ze sterowaniem HTTP 8099): dzień (Pogoda), wieczór (Jutro maks./min.), brak prognozy, brak trybu, wygaśnięcie `valid_until` bez delty → „brak prognozy” po 6 s, kafel Rolety A/B, panel z dwoma wierszami i Wróć; zamknij A → Stop A podczas oczekiwania → otwórz B: 3 `call_service` (`close_cover a`, `stop_cover a`, `open_cover b`); niedostępne światło: dotyk → 0 wywołań; uchwyt muzyki nad światłem → 0 wywołań, odsłonięty kafel → 1 `light.toggle`; kafle powiadomień pomarańczowe (zrzuty `.local/shots-088/*.png`).
- Nie sprawdzono: prawdziwe HA (pakiet, `weather.get_forecasts` z `continue_on_error` w trigger template, `this.attributes` po restarcie), publikacja i rollback na żywym pulpicie, fizyczne rolety/światło, zegar 0.8.8 na Lenovo, muzyka podczas otwierania panelu na zegarze, tryb 18:00 w prawdziwej strefie.

APK serwowany przez most (port 8757, `/helios.apk`) z `app/build/outputs/apk/debug/app-debug.apk`.
SHA256: af62a5c568cad9319f4f788d82d1ed2173f16fce0ccb3db8f38ae0fbb5b8a932
