# Helios 0.8.6 - redesign (SPEC 0.8a pkt 3-4; plan etap R)

- Baza: 0.8.5. Plan: docs/PLAN-0.8-implementation.md (R1-R5).
- R1 `Theme`: presety Ciepły grafit (domyślny) i Nocny błękit, promień 18, `raised` (tekst 10% nad powierzchnią) dla przycisków drugorzędnych; kontrast WCAG liczony statycznie (`ThemeTest`: wszystkie pary tekst/tło obu palet ≥ 4,5:1, także kafel 92% i zegar z osłoną 70% nad białym zdjęciem przyciemnionym 35% - najgorszy przypadek 0.8b). Pasek: HELIOS 17 / status 16 w kolorze pomocniczym, błąd w akcencie, ikona HA `#18BCF2` / pomocniczy. Stan offline kafla: pełna alfa, tekst pomocniczy i dopisek „(offline)” (bez `setAlpha(.55f)`, który psuł kontrast).
- R2 `TextFit` + `ClockLayout`: godzina dopasowana pomiarem fontu do szerokości (sufit 120) i do wysokości po odjęciu linii (data 24, dzień tygodnia 20, tytuł 18); przy niskim kaflu znika najpierw dzień tygodnia, potem data; pełna godzina zawsze ważniejsza od dolnej granicy. 2×2: 116 przy metrykach 0,6 em, wszystkie linie. Pogoda 44/18 z jednostką z encji (`DashboardSpec.attributes()` zachowuje `unit_of_measurement` encji temperatury; bez domyślnego °C). 1×1: nagłówek 17, wartość 24-28 z pomiaru, do 2 linii.
- R3 `MusicOverlay` wg geometrii 4.1 (`OverlayGeometry`, test: hitboxy 72×72 bez nakładania, wszystkie w panelu, uchwyt 728,204 dotyka prawej krawędzi i przecina tylko komórkę 4/2): nieprzezroczysty panel, chevron zamiast „Schowaj”, ikony wektorowe (`IconView`: previous/play/pause/next/stop/chevron/speaker/note), play z akcentem z okładki (`AccentColor.average` z 16×16, liczony raz na obraz na wątku sieciowym, bez AndroidX), płytka z nutką bez okładki, „Nieznany utwór” bez tytułu, animacja 180 ms `translationX` z uwzględnieniem `ANIMATOR_DURATION_SCALE`, dotyk poza panelem/uchwytem przechodzi do kafli. **Zmiana użytkownika w trakcie:** zamiast suwaka głośności suwak pozycji utworu (zegar ma sprzętowe przyciski): pozycja z `progress` Sendspin + upływ czasu, tick 1 s tylko przy otwartym panelu, puszczenie = `player_queues/seek`; błędy akcji trafiają do `MusicSnapshot.issue` („Muzyka: …”, „Przewijanie: …”), bez Toastów, czyszczone przy następnym `player_updated`.
- R4 dialogi w palecie przez `Theme.button/dialogColumn/label/tint`: potwierdzenie (własny `Dialog`, przyciski 56 dp), panel rolety, klawiatura parowania (jednostki 800×480), dialog urządzenia, menu, biblioteka muzyki (ikony transportu jak w panelu).
- R5: `music_stats` (underruny i zrzucone porcje) co 60 s podczas grania w dzienniku diagnostycznym; `http.keepAlive=false` (pobrania okładek/teł są jednorazowe; pula OkHttp oddawała zamknięty socket i drugi GET kończył się „unexpected end of stream”, bez ponowienia).
- Build, lint (0 błędów), 55 testów JVM: PASS (nowe: `ThemeTest` 4, `ClockLayoutTest` 5, `OverlayGeometryTest` 3, `DashboardSpecTest` atrybut jednostki).
- Emulator 800×480 (fixture HA `--v3 --hitbox`, fixture MA z okładką PNG na 8098, seek i tickami progress): dashboard, uchwyt, panel z okładką i akcentem, pauza, brak okładki, zmiana okładki, przewinięcie suwakiem (fixture dostała `player_queues/seek`, etykieta 2:51 / 3:00), panel rolety, potwierdzenie bramy, menu, dialog urządzenia, klawiatura, biblioteka. Hitbox: dotknięcie uchwytu (764,240) → 0 `call_service`, panel otwarty; dotknięcie odsłoniętej części kafla 4/2 (726,240) → dokładnie 1 `light.toggle`; krawędź uchwytu (730,240) → 0 dodatkowych wywołań; lewy kafel przy zamkniętym panelu → 1 wywołanie. Zrzuty: `.local/shots-086/*.png`.
- Nie sprawdzono: fizyczny Lenovo (RAM `dumpsys meminfo`, underruny `music_stats` przed/po, 30 min muzyki, prawdziwe okładki `imageproxy`), preset Nocny błękit na urządzeniu (tylko test kontrastu), zegar 2×1 z tytułem na ekranie (tylko `ClockLayoutTest`). Rola `artwork@v1` nadal nieogłaszana.

APK serwowany przez most (port 8757, `/helios.apk`) z `app/build/outputs/apk/debug/app-debug.apk`.
SHA256: 3cea2c6f6beec02b2be1e9ffcb40832ea26527a20e773549b904cd15122f4fcf
